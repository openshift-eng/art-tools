# Snyk (https://snyk.io) policy file
version: v1.0.0
exclude:
  global:
    - art-cluster/cloudflare/src/index.ts
    # False positive SSRF (javascript/Ssrf): the fetch target is validated
    # with an HTTPS-only scheme check and a strict domain allowlist before
    # any request is made.