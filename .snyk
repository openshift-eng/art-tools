# Snyk (https://snyk.io) policy file
# Snyk Code (SAST) does not honor ignore: rules; use exclude: to skip paths.
# Cloudflare worker proxy uses allowlist validation; SSRF finding is a false positive.
version: v1.0.0
exclude:
  global:
    - art-cluster/cloudflare/**
